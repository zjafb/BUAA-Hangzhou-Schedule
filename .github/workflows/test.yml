name: Test Compilation

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:
    inputs:
      ios_only:
        description: 仅验证完整 iOS App 的未签名归档
        type: boolean
        default: false

env:
  UPDATE_DOWNLOAD_URL: https://github.com/BUAASubnet/UBAA/releases

jobs:
  test-android:
    name: Test Android
    if: github.event_name != 'workflow_dispatch' || !inputs.ios_only
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Compile Android
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
        run: ./gradlew :androidApp:assembleDebug

  test-desktop-linux:
    name: Test Desktop Linux
    if: github.event_name != 'workflow_dispatch' || !inputs.ios_only
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Compile Desktop
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
        run: |
          ./gradlew :shared:compileKotlinMetadata
          ./gradlew :composeApp:compileKotlinJvm

  test-server:
    name: Test Server
    if: github.event_name != 'workflow_dispatch' || !inputs.ios_only
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Compile Server
        env:
          SERVER_BIND_HOST: ${{ secrets.SERVER_BIND_HOST || '0.0.0.0' }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          USE_VPN: ${{ secrets.USE_VPN || 'false' }}
        run: ./gradlew :server:compileKotlin

  test-web-wasm:
    name: Test Web Wasm
    if: github.event_name != 'workflow_dispatch' || !inputs.ios_only
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Compile Web Wasm
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
        run: ./gradlew :composeApp:compileKotlinWasmJs

  test-web-js:
    name: Test Web JS
    if: github.event_name != 'workflow_dispatch' || !inputs.ios_only
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Compile Web JS
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
        run: ./gradlew :composeApp:compileKotlinJs

  test-ios:
    name: Test iOS
    runs-on: macos-15
    timeout-minutes: 75
    env:
      DEVELOPER_DIR: /Applications/Xcode_26.3.app/Contents/Developer
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 校验 iOS 发布元数据与签名规则
        run: python3 -m unittest discover -s .github/scripts -p 'test_ios_release.py'
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: 验证完整 iOS App 归档
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
        run: |
          xcodebuild -version
          xcodebuild archive \
            -project iosApp/iosApp.xcodeproj \
            -scheme iosApp \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$RUNNER_TEMP/UBAA-unsigned.xcarchive" \
            -derivedDataPath "$RUNNER_TEMP/ubaa-ios-derived-data" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=

  test-windows:
    name: Test Windows
    if: github.event_name != 'workflow_dispatch' || !inputs.ios_only
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Compile Windows
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
        run: ./gradlew :composeApp:compileKotlinJvm
